pandas>=2.2
pyyaml>=6.0
pymupdf>=1.24
pillow>=10.0
numpy>=1.26
matplotlib>=3.8
streamlit>=1.34
altair>=5.0
pyarrow>=15.0
pytest>=8.0

# OCR engines. PaddleOCR is the primary engine; EasyOCR is the fallback.
# Install may take time because both include deep-learning dependencies.
paddleocr>=2.7
paddlepaddle>=2.6
easyocr>=1.7
